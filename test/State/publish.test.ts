// import {publish} from '../../src/State/publish';
// import {subscribe} from '../../src/State/subscribe';
// import {events} from '../../src/State/events';

// @TODO Test publish I have no idea how to test this
describe('Publish function', () => {
  // beforeEach(() => {
  //   subscribe('test', (arg: any) => console.log(arg));
  // });

  // afterEach(() => {
  //   events.clearEvents();
  // });

  it('it publishes an event', () => {
    // publish('test', 'elo');
    expect('')
  });
});